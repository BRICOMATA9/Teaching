#include <stdio.h>
main() {
	int n, y;
	double x;
	printf("donez un nombre\n");
	scanf("%u", y);
	printf("donnez le nombre d'iteration\n");
	scanf("%u", n);
	printf("donnez une aproximation\n");
	scanf("%f", x);
	for (int i = 0, i < n, i++)
		x += 2x - (y * x * x);
}
